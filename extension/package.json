{
  "name": "construct-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsup && cp static/* dist/",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
